# Production 기본 설정
# nginx만 실행, hotload server 없음

services:
  cv-service:
    build:
      context: .
      target: nginx
    expose:
      - "80"
    restart: unless-stopped
    networks:
      - default
      - gateway

networks:
  gateway:
    external: true
    name: my-server_gateway
