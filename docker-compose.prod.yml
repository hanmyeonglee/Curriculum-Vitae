# Production 전용 설정
# docker compose -f docker-compose.yml -f docker-compose.prod.yml up

services:
  cv-service:
    expose:
      - "80"
    restart: always
    networks:
      - gateway

networks:
  gateway:
    external: true
    name: my-server_gateway
